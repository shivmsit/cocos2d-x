pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = 'HelloCpp'
include ':libcocos2dx'
project(':libcocos2dx').projectDir = file('../cocos2d/cocos/platform/android/libcocos2dx')
include ':app'
