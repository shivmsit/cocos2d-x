pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = 'LuaEmptyTest'
include ':libcocos2dx'
project(':libcocos2dx').projectDir =
    file('../../../../cocos/platform/android/libcocos2dx')
include ':app'
